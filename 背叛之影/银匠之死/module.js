const path = "/背叛之影/银匠之死";
// 生成链接的函数

const replaceDirs = [];
const repalceFiles = [
    `.${path}/src.md`,
];
const repalceNameFiles = [`.${path}/names.txt`,];

module.exports = {
    replaceDirs: replaceDirs,
    repalceFiles: repalceFiles,
    repalceNameFiles: repalceNameFiles,
}