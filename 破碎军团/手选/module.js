const path = "/破碎军团/手选";

const replaceDirs = [];
const repalceFiles = [`.${path}/base.md`, `.${path}/src.md`,];
const repalceNameFiles = [`.${path}/names.txt`,];

export {
    replaceDirs,
    repalceFiles,
    repalceNameFiles,
}