const path = "/背叛之遗/向死者致敬";
// 生成链接的函数

const replaceDirs = [];
const repalceFiles = [
    `.${path}/src.md`,
];
const repalceNameFiles = [`.${path}/names.txt`,];

export {
    replaceDirs,
    repalceFiles,
    repalceNameFiles,
}