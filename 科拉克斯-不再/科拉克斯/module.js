const path = "/科拉克斯-不再/科拉克斯";

const replaceDirs = [];
const repalceFiles = [`.${path}/base.md`, `.${path}/src.md`,];
const repalceNameFiles = [`.${path}/names.txt`,];

export {
    replaceDirs,
    repalceFiles,
    repalceNameFiles,
}