const path = "/考斯印记/无迹之旅";

const replaceDirs = [];
const repalceFiles = [`.${path}/base.md`, `.${path}/src.md`,];
const repalceNameFiles = [`.${path}/names.txt`,];

export {
    replaceDirs,
    repalceFiles,
    repalceNameFiles,
}