const path = "/围城先驱/万变";

const replaceDirs = [];
const repalceFiles = [
    `.${path}/src.md`,
    `.${path}/base.md`
];
const repalceNameFiles = [`.${path}/names.txt`,];

export {
    replaceDirs,
    repalceFiles,
    repalceNameFiles,
}