const path = "/塔兰/塔兰-见证者";

const replaceDirs = [];
const repalceFiles = [
    `.${path}/src.md`,
    `.${path}/base.md`,
];
const repalceNameFiles = [`.${path}/names.txt`,];

export {
    replaceDirs,
    repalceFiles,
    repalceNameFiles,
}