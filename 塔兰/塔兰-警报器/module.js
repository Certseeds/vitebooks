const path = "/塔兰/塔兰-警报器";

const replaceDirs = [];
const repalceFiles = [
    `.${path}/src.md`,
    `.${path}/base.md`,
];
const repalceNameFiles = [`.${path}/names.txt`,];

export {
    replaceDirs,
    repalceFiles,
    repalceNameFiles,
}